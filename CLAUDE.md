# pi-spectrum

Single-user iMessage agent bot (built on Spectrum/Photon). Only ever talks to one person at a time - don't add multi-conversation/multi-user isolation (e.g. per-conversation agent instances, scoped memory) unless explicitly asked.
